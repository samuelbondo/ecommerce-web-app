# This script was used once to create the banners table and seed default data.
# It has been removed to avoid committing credentials.
# To re-run, use server/setup-db.js with environment variables set.
