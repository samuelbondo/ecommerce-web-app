# This script was used once to update product image URLs in the database.
# It has been removed to avoid committing credentials.
# To update images, connect to your database and run:
#
# UPDATE products SET image_url = 'https://images.unsplash.com/...' WHERE id = 1;
